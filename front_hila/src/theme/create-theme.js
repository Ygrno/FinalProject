import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    direction: 'rtl',
    palette: {
        primary: {
            main: "#3d4661"
        },
        secondary: {
            main: "#76d1ff"
        },
        card: '#e5eded'
    }
});

export default theme;