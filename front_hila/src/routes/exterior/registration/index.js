import { Registration } from './Registration';
import { UserType } from '../../../constants';

export default {
    path: "/register",
    allowedUserTypes: [],
    component: Registration,
    title: 'הרשמה'
};