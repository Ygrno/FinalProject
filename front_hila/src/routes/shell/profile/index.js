import { Profile } from './Profile';
import { UserType } from '../../../constants';

export default {
    path: "/profile",
    allowedUserTypes: [],
    component: Profile,
    title: 'הפרופיל שלי'
};

